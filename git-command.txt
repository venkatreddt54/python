Intialize the Repository:
git clone --mirror https://github.com/venkatreddt54/python.git .git
git cofig --bool core.bare false
git reset --hard

Pushing Data to Repository:
git add .
git commit -m "message"
git push